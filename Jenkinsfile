pipeline {
    agent any
    stages {
        stage('Get Branch') {
            steps {
                script {
                   // Get the current branch using git commands
                   echo "Current Branch: ${env.BRANCH_NAME}"

                }
            }
        }

    }

    
}
